# Frontend

This is the frontend for the ecommerce relationship demo.

## Available Scripts

In the frontend directory, you can run:

### `npm start`

Starts the development server.

### `npm test`

Runs tests in watch mode.

### `npm run build`

Builds the production app into the `build` folder.
